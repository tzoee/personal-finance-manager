fix: improve cloud sync to handle all data fields properly

- Change setLocalStorageData to write arrays even if empty
- This ensures clean sync when downloading from cloud
- Previously, empty arrays were skipped which caused sync issues

Note: Users need to re-upload from device with data after this fix