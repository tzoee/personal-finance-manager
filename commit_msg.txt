fix: FAB click issue on mobile and cashflow data display

Bug fixes:
1. FAB (Floating Action Button) - action buttons now clickable on mobile
   - Fixed z-index layering (backdrop z-40, FAB z-50)
   - Separated backdrop from FAB container
   - Added stopPropagation and setTimeout for reliable navigation
   - Increased button size from w-10 to w-12 for better touch targets

2. Cashflow chart - now shows correct monthly data
   - Changed from parseISO(getCurrentDate()) to new Date()
   - Fixed transaction filtering for each month
   - Previous month data now displays correctly in January