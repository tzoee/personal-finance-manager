fix: improve cloud sync reliability

- Use sessionStorage to track sync per session
- Auto-save triggers on all data changes (wishlist, installments, etc)
- Better loading state management
- Reduce auto-save delay to 3 seconds
